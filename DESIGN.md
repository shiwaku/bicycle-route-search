# bicycle-route-search 設計書

東京都・埼玉県を対象に、**自転車に適した道路を優先する経路探索**を作る。
道路網は国土数値情報（KSJ）の道路中心線、道路の性質は道路交通センサス（R03）と JARTIC の交通規制情報から与える。

- 状態：設計（2026-09-25）。まだ実装していない
- 位置づけ：**自転車ナビゲーションシステムをゼロから作るときの土台**。このリポジトリの第1版は東京・埼玉の経路探索だが、データの持ち方とパイプラインは、全国展開・曲がり角の案内・右左折のコストを後から足せる形にする（§12）
- 関連リポジトリ：
  - [ksj-route-search-api](https://github.com/shiwaku/ksj-route-search-api)：車の全国経路探索。KSJ を読む処理、scipy による Dijkstra の構成を参考にする
  - [jartic-traffic-regulation-converter](https://github.com/shiwaku/jartic-traffic-regulation-converter)：交通規制情報の取得・フォーマットの知見
  - mlit-road-traffic-census-converter：センサスの GeoParquet を作ったもの

---

## 1. 「自転車に適した道路」の定義

利用者の要件は次の4つ。それぞれを、データから計算できる指標に置き換える。

| 要件 | 指標 | 主なデータ |
|---|---|---|
| 交通量が少ない | 自動車類交通量。時刻を指定すればその時間帯の値、指定しなければ24時間の値 | センサス（時間帯別交通量表、箇所別基本表） |
| 幅員が広い | 幅員区分 | KSJ `N13_006` |
| 信号交差点が少ない | 経路上で通る信号の数 | JARTIC `98 信号機` |
| 歩道がある | 交通量が多いときに逃げ込める場所があるか（下記） | センサス設置率、JARTIC `81` `82` `110` `111` `2` `3` |

加えて、**自転車が通れない道は探索に使わない**。

### 「歩道がある」の意味

利用者の定義は「**比較的広い道路で、交通量が多い時間帯は歩道に退避できる**」こと。つまり歩道は、それ自体がよさなのではなく、**交通量の悪さを打ち消す**ものとして効く。そこで歩道を独立した項にはせず、交通量の項を割り引く係数として扱う（§6）。

法令上も、この退避は認められている。道路交通法第63条の4第1項は、普通自転車が歩道を通行できる場合として次の3つを定める（[e-Gov 法令API](https://laws.e-gov.go.jp/api/2/law_data/335AC0000000105)、2026-07-21 施行版で確認）。

1. 道路標識等で歩道通行が認められているとき（JARTIC `81`）
2. 運転者が児童・幼児などのとき
3. **車道または交通の状況に照らして、通行の安全を確保するため歩道を通行することがやむを得ないと認められるとき**

利用者のいう「退避」は第3号に当たるので、`81` の標識が無い歩道も退避先に数える。ただし同条第2項により、歩道では車道寄りを**徐行**し、歩行者の通行を妨げるときは一時停止しなければならない。このため歩道があっても交通量の悪さを完全には打ち消さず、`81` があるほうをより高く評価する。

- 通行禁止のうち自転車に適用されるもの
- 一方通行のうち自転車を除外していないものの逆走
- KSJ の石段・建設中
- 高速道路・有料道路

### データの範囲が条件ごとに違う

センサスの対象は主に国道・都道府県道なので、次の状態になる。

- **幹線道路**：4条件すべてのデータがある。幅員が広く歩道もあることが多いが、交通量が多い
- **生活道路**：交通量は少ないはずだが、交通量と歩道の**データがない**。幅員は狭いことが多い

この非対称を隠さず、センサスが無い道路は「不明」として扱い、推定値で補う（§6.3）。4条件を全部満たす道で経路がつながることは期待しない。

---

## 2. 入力データ

| データ | 版 | 手元の場所 | 範囲 |
|---|---|---|---|
| KSJ 道路中心線（N13） | N13-24 | `../ksj-route-search-api/input/geoparquet/N13-24_{5338,5339,5438,5439}.parquet` | 全道路 |
| 道路交通センサス 一般交通量調査（箇所別基本表） | R03（令和3年度＝2021年調査） | `../mlit-road-traffic-census-converter/data/r03/output/traffic_census_2021_converted.parquet` | 調査対象区間のみ |
| 同 時間帯別交通量表 | R03 | `../mlit-road-traffic-census-converter/data/r03/csv/jikantai/zkntrf{11,13}.csv`（cp932） | 交通量を観測した区間のみ |
| JARTIC 交通規制情報（typeD） | 拡張版標準フォーマット k_2.1、2026年7月分 | `../jartic-traffic-regulation-converter/work/zip/typeD_{tokyo,saitama}.zip` | 規制がある場所のみ |
| 行政区域（都県境） | N03-20240101 | `../jci/N03_行政区域（ポリゴン）/GeoJSON/N03-20240101_prefecture.geojson` | — |

入力ファイルは git に入れず、パスは `config.toml` で指定する。

**JARTIC は変換済みの GeoJSONL ではなく、元の CSV を読む。** 変換済みのデータには対象車両コード `1_A` しか残っておらず、自転車の判定に要る `_D` 列と除外車両コードが無いため（§4.2）。

---

## 3. 前提と割り切り

| # | 前提 | 理由 |
|---|---|---|
| ① | 対象は東京都（島しょ部を除く）と埼玉県 | 島しょ部は本土と道路網がつながらない |
| ② | 幅員で道路を絞らない | 自転車は細い道も走る。ksj-route-search-api の「5.5m以上」フィルターは使わない |
| ③ | 時刻を指定しないとき、時間帯付きの規制は**常時かかるものとして扱う**。時刻を指定したときは、その時刻にかかる規制だけを使う | 安全側（通れない側）に倒す。時刻は交通量（§4.3）と規制の両方に使う |
| ④ | 第1版の探索では、右左折禁止（`12` 指定方向外進行禁止など）と右左折のコストを使わない | 辺ベースのグラフが必要で、グラフ構造が変わる。**ただしデータは第1版から抽出して持つ**。ナビでは必須なので第2版で使う（§12.3） |
| ⑤ | 一時停止・信号の待ち時間は固定値 | 信号のサイクルは使わない |
| ⑥ | センサスは2021年の値 | 最新版。R07 が出たら差し替える |
| ⑦ | 坂道（勾配）は扱わない | 要件に無い。DEM で後から足せるようにコストを項の和にしておく |

---

## 4. データごとの取り込み方

### 4.1 KSJ 道路中心線

コード定義は国土数値情報の一次情報で確認した（[N13 属性定義](https://nlftp.mlit.go.jp/ksj/gml/datalist/KsjTmplt-N13-2024.html)）。

| 列 | 意味 | 使い方 |
|---|---|---|
| `N13_002` 種別 | 1 通常部 / 2 庭園路 / 3 徒歩道 / 4 石段 / 5 不明 | 4 は除外。3 は割増（§6.2） |
| `N13_003` 道路分類 | 1 国道 / 2 都道府県道 / 3 市区町村道等 / 4 高速自動車国道等 / 5 その他 / 6 不明 | 4 は除外。センサスが無いときの交通量の推定に使う |
| `N13_004` 道路状態 | 1 通常部 / 2 橋・高架 / 3 トンネル / 4 雪囲い / 5 建設中 / 6 その他 / 7 不明 | 5 は除外 |
| `N13_006` 幅員区分 | 1 3m未満 / 2 3〜5.5m / 3 5.5〜13m / 4 13〜19.5m / 5 19.5m以上 / 6 不明 | 幅員の指標 |
| `N13_007` 有料区分 | コードリスト未確認（5339 では 1: 193.7万、2: 0.66万） | 意味を確かめてから有料区間を除外する |

> 注：`ksj-road-makenetwork` の手順書は `N13_002` を「上下線区分（4=一方通行）」と書いているが、一次情報では「種別（4=石段）」。本リポジトリは一次情報に従う。

処理：

1. 4メッシュを読み、東京都（北緯35.4度以北・東経140度以西）と埼玉県の範囲に交わるリンクを残す
2. 除外条件に当たるリンクを落とす
3. 端点の座標（1e-7度に丸める）でノードを作る
4. リンク長は平面直角座標系IX系（EPSG:6677）で測る
5. 始点・終点は、1,000ノード以上の連結成分にだけスナップする（ksj-route-search-api と同じ）

### 4.2 JARTIC 交通規制情報

#### 使う規制種別

| 用途 | コード | 東京（行数） | 埼玉（行数） |
|---|---|---|---|
| 信号の数 | `98` 信号機 | 15,972 | 10,364 |
| 一時停止の待ち | `63` 一時停止 | 141,178 | 125,982 |
| 通れない（自転車に適用されるもの） | `1` `4` `5` `7` `19` | §4.2 下の表 | 同左 |
| 一方通行（自転車に適用されるもの） | `11` | 2,234 | 1,351 |
| 自転車の通行空間 | `2` `3`、`110` `111`（普通自転車が対象のもの） | 111：293 | 3：2,003 |
| 歩道を走れる | `81` 歩道通行可、`82` 歩道通行部分 | 333 / 132 | 2,339 / 43 |

行数は CSV の行数そのままで、重複は取り除いていない。同じ地点が複数行ある可能性があるため、取り込み時に座標で重複を除く。

#### 自転車に適用されるかの判定（推定）

対象・除外車両は、`対象車両コード{1..5}_{A..D}` と `除外車両コード{1..5}_{A..D}` に入っている。値は10進の各桁が1つの車種を表し、同じカテゴリに複数の車種があるときは足し合わされる（仕様書 3.7.4）。**桁と車種の対応表（別記2 共通コード表）は JARTIC で公開されているものが見つからなかった**ため、全国47都道府県の実データで「どの桁がどの規制名に立つか」を集計して推定した。

| 桁 | 推定 | 根拠（全国の件数） |
|---|---|---|
| A の1桁目 | 車両（自転車を含む） | 一時停止・駐車禁止・一方通行などの対象の大半（112万件） |
| A の2桁目 | 自動車（自転車を含まない） | 「二輪の自動車以外の自動車通行止め」など |
| A の10桁目 | 軽車両 | 「軽車両通行止め」の対象（103件） |
| D の3桁目 | 普通自転車 | 対象としては「普通自転車歩道通行可」（転歩 35,093件ほか）、「自転車横断帯」、「普通自転車専用通行帯」、「自転車一方通行」に立つ。除外としては「一方通行」（25,099件ほか）、「通禁（自転車等歩行者用）」に立つ |

判定ルール：

```
適用 = 対象セットのどれかで (A の1桁目 or A の10桁目 or D の3桁目) が立つ
     かつ 除外セットのどれにも D の3桁目が立たない
```

対象車両が全部空の行は、自転車には適用しないとみなし、件数をレポートに残す。この判定で東京・埼玉を数えた結果：

| コード | 都県 | 自転車に適用（終日 / 時間帯） | 自転車は除外（終日 / 時間帯） | 自転車は対象外 | 対象車両が空 |
|---|---|---|---|---|---|
| `1` 通行禁止 | 東京 | 38 / 83 | 1,983 / 4,308 | 4 | 21 |
| `4` 通行禁止 | 東京 | 402 / 294 | 2,043 / 4,385 | 6,138 | 108 |
| `11` 一方通行 | 東京 | 1,501 / 733 | 4,828 / 11,620 | 153 | 1 |
| `11` 一方通行 | 埼玉 | 1,173 / 178 | 2,617 / 104 | 676 | 0 |
| `5` 通行止め | 埼玉 | 0 / 45 | 0 / 8 | 1,432 | 11 |
| `7` 踏切 | 埼玉 | 23 / 0 | 18 / 0 | 346 | 0 |

東京の一方通行は9割近く（87%）が「自転車を除く」。自転車の経路では、多くの一方通行を双方向に使える。

**この推定は実装の最初に検証する**（§8-1）。共通コード表が手に入ったら差し替える。

#### 一方通行の向き

一方通行の向きは座標の並び順と `指定・禁止方向の別コード`（仕様書 NO.40「一方通行における座標の格納順を示すコード」）で決まる。

| 都道府県 | コード値 | 並び順の意味 |
|---|---|---|
| 北海道 | 1 | 通行方向と**逆**。OSM と突き合わせて51件中50件で確認済み（jartic-traffic-regulation-converter README） |
| 埼玉 | 1 | 北海道と同じ値なので、逆と推定 |
| 東京 | 2 | 並び順どおりと推定 |

全国を見ると、値は都道府県ごとにどちらか一方にそろっている（`1` が31都道府県、`2` が16都道府県。静岡だけ両方が混在）。東京・埼玉の向きは、実装時に OSM の `oneway` と突き合わせて確かめる（§8-1）。

#### その他

- `データ更新区分コード` は東京が空、埼玉が 5（269,472行）と 4（16行）。値の意味は共通コード表が無いため未確認。廃止を表す値が出てきたら除外する
- `規制時間1_開始` が入っているものを「時間帯付き」とする

### 4.3 道路交通センサス（R03）

使う列：

| 列 | 使い方 |
|---|---|
| `２４時間自動車類交通量（上下合計）／合計（台）` | 交通量の指標（時刻を指定しないとき） |
| `交通量／都道府県指定市コード`、`交通量／調査単位区間番号` | 時間帯別交通量表と結合するキー |
| `交通安全施設等／歩道設置率（％）`、`自転車歩行者道設置率（％）` | 歩道の指標 |
| `交通安全施設等／自転車道設置率（％）`、`自転車レーン設置率（％）` | 自転車の通行空間 |
| `交差点密度／信号交差点（箇所／ｋｍ）` | JARTIC の信号の数との突き合わせ（検証用） |
| `幅員構成／車道幅員（ｍ）` | KSJ の幅員区分との突き合わせ（検証用） |

**時間帯別交通量表**は、1区間につき「上り・下り × 車種区分」の行があり、7時台から翌6時台までの24列（台/時）を持つ。これを区間ごとの上下合計・全車種の時刻別交通量 `V_h`（h = 0〜23）にまとめる。

- 時間帯別の値が無い区間（非観測区間など）は、`V_24` に同じ都県の観測区間の平均時刻分布を掛けて推定し、推定であることをフラグで持つ
- 時刻を指定した探索では `V_h × 24` を `V_24` の代わりに使う。24時間の平均と同じ尺度にそろえ、同じしきい値（§6.2）を使うため

センサス区間は DRM 由来の線で、KSJ の中心線とは形が一致しない。§5 の線マッチングで KSJ リンクに属性を移す。1本の KSJ リンクに複数の区間が重なったときは、重なりが最も長い区間を採る。

---

## 5. マップマッチング

どのデータも KSJ とは ID でつながらないため、位置で対応づける。座標はすべて EPSG:6677 に投影してから計算する。

### 5.1 線（JARTIC の線規制、センサス区間）→ KSJ リンク

1. 規制線にバッファをかける（JARTIC は 8m、センサスは 15m）
2. バッファと交わる KSJ リンクを、STRtree で候補として取る
3. 次の2条件を満たすものだけを対応とみなす
   - **被覆率** ＝ バッファ内にあるリンク長 ÷ リンク長 ≥ 0.6
   - **平行度** ＝ |規制線上に射影したリンク終点の位置 − 始点の位置| ÷ リンク長 ≥ 0.7。交差する道路の短いリンクを拾わないため
4. 向き：射影した位置が終点のほうが大きければ、リンクは規制線と同じ向き。一方通行ではこれを使う

### 5.2 点（信号、一時停止）→ KSJ ノード

最寄りのノードが 20m 以内にあれば対応させる。1つのノードには、信号・一時停止をそれぞれ最大1つまで数える。

### 5.3 パラメータ

バッファ幅・被覆率・平行度・スナップ距離は仮の値。検証（§8-2）で対応率と誤対応を見て決める。

---

## 6. コストモデル

### 6.1 式

有向リンク e（ノード u → v、長さ L メートル）のコスト：

```
cost(e) = L × (1 + α_t·f_t·(1 − r) + α_w·f_w + α_p·f_p)
        + β_sig·signal(v) + β_stop·stop(v)
```

- `f_*` は 0（よい）〜1（悪い）
- `r` は**退避のしやすさ**で、0（逃げ場なし）〜1未満
- α は重み（無次元）
- β はメートル換算の待ちペナルティ

歩道は交通量の項 `f_t` だけを割り引く。交通量が少ない道では、歩道の有無は経路に効かない。これが「交通量が多い時間帯は歩道に退避できる」の定義（§1）をそのまま式にした形になる。

**全部の重みを 0 にすると最短距離になる**。比較の基準として同時に返す。

### 6.2 各項

| 項 | 条件 | 値 |
|---|---|---|
| `f_t` 交通量 | センサスあり | `clip((V − 4,000) / (20,000 − 4,000), 0, 1)`。V は時刻を指定すれば `V_h × 24`、しなければ `V_24`（§4.3） |
| | センサスなし・国道／都道府県道 | 0.7（幹線だが値が不明） |
| | センサスなし・それ以外 | 0.1（生活道路とみなす） |
| `r` 退避のしやすさ | 自転車道・自転車レーン・自転車用道路（JARTIC `2` `3` `110` `111`、またはセンサスの自転車道・レーン設置率 ≥ 50%） | 0.8 |
| | センサスあり：自歩道の率 j、歩道の率 s（s は自歩道を含む） | `0.7·j/100 + 0.4·(s − j)/100` |
| | JARTIC `81` `82`（普通自転車歩道通行可）が対応 | 上の値と 0.7 の大きいほう |
| | センサスなし・国道／都道府県道 | 0.5（下記の実測から） |
| | センサスなし・それ以外 | 0（`f_t` が小さいので影響は小さい） |
| `f_w` 幅員 | KSJ `N13_006` | 1→1.0、2→0.6、3→0.3、4→0.1、5→0.0、6→0.6 |
| `f_p` 通路種別 | KSJ 徒歩道（`N13_002=3`） | 1.0、それ以外 0 |
| `signal(v)` | v に信号がある | 1 / 0 |
| `stop(v)` | v に一時停止がある | 1 / 0 |

`r` の係数の考え方：

- **自歩道 0.7**：標識で認められた歩道（道交法第63条の4第1項第1号）。ただし徐行義務があるので1にはしない
- **自歩道でない歩道 0.4**：「やむを得ないとき」の退避（同第3号）。法令上は通れるが、歩行者優先がより強い
- **自転車道・レーン 0.8**：車から分けられた自転車の空間

#### 実測：交通量が多い道路の歩道は、ほぼ自転車も通れる

センサス R03 の東京・埼玉の区間（高速道路を除く）を24時間交通量の帯で分け、延長で重み付けして設置率を比べた。歩道の率は自歩道を含むとして読んだ（どの帯でも自歩道 ≤ 歩道なので、この読みと整合する）。

| 24時間交通量 | 東京：歩道 / うち自歩道 | 埼玉：歩道 / うち自歩道 |
|---|---|---|
| 4,000台未満 | 39% / 17% | 42% / 34% |
| 4,000〜1万台 | 81% / 55% | 72% / 59% |
| 1万〜2万台 | 91% / 75% | 83% / 70% |
| 2万〜4万台 | 97% / 94% | 86% / 81% |
| 4万台以上 | 85% / 84% | 59% / 51% |

2万台以上では、歩道のうち自歩道が占める割合が東京 97〜98%、埼玉 86〜94%。**交通量が多い道路の歩道は、だいたい自転車通行可**になっている。

一方、JARTIC の `81` は東京で333行、埼玉で2,339行しかなく、この実態と量が合わない。そこで**退避の判定はセンサスの自歩道の率を主とし、`81` は補助とする**。センサスが無い国道・都道府県道の `r = 0.5` は、上の表の中ほど（1万〜2万台：`0.7×0.75 + 0.4×0.16 ≒ 0.59`、`0.7×0.70 + 0.4×0.13 ≒ 0.54`）から控えめに取った。

### 6.3 既定の重み

| 重み | 既定値 | 考え方 |
|---|---|---|
| α_t | 2.0 | 逃げ場の無い交通量の多い道は、最大で距離の3倍相当に見る |
| α_w | 1.0 | |
| α_p | 2.0 | 押して歩く区間とみなす |
| β_sig | 125 m | 平均待ち30秒 × 15 km/h（250 m/分） |
| β_stop | 20 m | 停止・再発進に約5秒 |

しきい値（4,000台・20,000台など）、`r` の係数、重みは仮の値。経路の例で見比べて調整する（§8-3）。

### 6.4 通れない辺

グラフから除く辺：

- 自転車に適用される通行禁止（`1` `4` `5` `7` `19`）に対応したリンク：両方向
- 自転車に適用される一方通行に対応したリンク：逆方向だけ
- 時間帯付きのもの：時刻の指定が無ければ除く。指定があれば、その時刻に規制がかかるときだけ除く

---

## 7. 探索と API

### 7.1 グラフ

- 有向グラフを scipy の CSR 形式で持つ。構造（どのノード間に辺があるか）は起動時に一度だけ作る
- 重みはリクエストごとに numpy で計算し直す。辺は数百万本の規模で、数十ミリ秒の見込み
- **並行リンク**（同じノード対を結ぶ複数のリンク）の扱い：csgraph は同じ (u, v) の値を足してしまう。そこで (u, v) で並べ替えた順序を起動時に作っておき、`np.minimum.reduceat` で最小値を採る。経路を組み立てるときは、各ノード対でコスト最小のリンクを選び直す
- Dijkstra：`scipy.sparse.csgraph.dijkstra(indices=始点, return_predecessors=True)`

### 7.2 エンドポイント（FastAPI）

| メソッド・パス | 内容 |
|---|---|
| `GET /health` | リンク数・ノード数・起動時間 |
| `GET /route` | `from`・`to`（経度,緯度）、`hour`（0〜23、省略可）、`alpha_t` `alpha_w` `alpha_p` `beta_sig` `beta_stop`。**おすすめ経路と最短経路**の2本を返す |
| `GET /links` | `bbox` 内のリンクと `f_t` `r` `f_w`（地図の色分け用。`hour` も指定可。件数に上限を設ける） |

`/route` が経路ごとに返すもの：

- GeoJSON の線
- 距離
- 推定所要時間：15 km/h ＋ 信号1つあたり30秒
- 通る信号の数・一時停止の数
- 距離のうちの割合：交通量が多い（`f_t ≥ 0.5`）、そのうち退避できる（`r ≥ 0.5`）、幅員5.5m以上、データが不明

### 7.3 画面

`web/index.html` の1枚で作る（MapLibre GL JS、背景は地理院の淡色地図）。

- 地図のクリックで出発地・目的地を置く
- 出発時刻（時）の指定。指定しなければ24時間の値を使う
- 重みのスライダーと、プリセット（最短 / おすすめ / 幹線を避ける）
- 2本の経路の比較表
- ズーム14以上で、リンクを「逃げ場の無い交通量」`f_t·(1 − r)` / `f_t` / `r` / `f_w` のどれかで色分けする

---

## 8. 検証計画

| # | 確かめること | 方法 | 合格の目安 |
|---|---|---|---|
| 0 | 時間帯別交通量表とセンサス区間の結合 | 結合キーで結合できた区間の割合を出す | 観測区間の 95%以上 |
| 1 | 一方通行の向きと、自転車適用の判定 | 東京・埼玉それぞれ50件以上を抽出し、OSM の `oneway`・`oneway:bicycle` と突き合わせる | 向きの一致 90%以上。外れたら都県ごとに反転する |
| 2 | マッチングの対応率と誤対応 | 対応率を種別ごとにレポートに出す。センサスの幅員と KSJ の幅員区分の一致を見る。地図で目視する | JARTIC の線 80%以上、センサス 80%以上、信号 90%以上 |
| 3 | 経路の妥当性 | 都心・郊外・県境をまたぐ OD を3組以上用意し、最短経路と比べる | 通れない辺を通らない。重みを変えると経路が期待どおりに変わる |
| 4 | 速度 | `/route` の応答時間 | 2秒以内 |

---

## 9. 既知の限界

- **交差点の右左折禁止を扱わない**（前提④）
- **生活道路の交通量・歩道は推定値**。狭い道が安全かどうかは、実際には路上駐車や通り抜けの車で変わる
- **JARTIC の規制はデータベースの写し**。仕様書の注意事項のとおり、実際の標識と一致しない場合がある。画面に「実際の標識に従う」ことを明記する
- **センサスは2021年の値**。以降の道路の整備は反映されない
- **対象車両コードの桁の意味は推定**（§4.2）
- **歩道の設置率は区間全体の値**。道路のどちら側に歩道があるか、区間のどこにあるかは分からない。上り・下りで同じ `r` を使う
- **退避は歩行者が少ないことが前提**。歩道の混雑（駅前・商店街など）は見ていない

---

## 10. 決まったこと・決まっていないこと

| 項目 | 決定 |
|---|---|
| 「歩道がある」の意味 | **決定**：比較的広い道路で、交通量が多い時間帯は歩道に退避できること。交通量の項を割り引く `r` として扱う（§1、§6） |
| 時間帯付き規制 | **決定**：時刻の指定が無ければ常にかかるものとして扱い、指定があればその時刻で判定する（前提③） |
| 公開 | **決定**：GitHub で公開（https://github.com/shiwaku/bicycle-route-search） |
| `r` の係数・しきい値・重み | 未定。仮の値で実装し、経路の例で調整する（§8-3） |

---

## 11. リポジトリ構成（予定）

```
bicycle-route-search/
├── DESIGN.md
├── config.toml            入力データのパス・対象都道府県
├── preprocess/
│   ├── build_network.py   KSJ → リンク／ノード（§4.1、§12.2 の安定 ID）
│   ├── load_jartic.py     JARTIC CSV → 規制（点・線）＋自転車適用の判定（§4.2）
│   ├── match.py           マップマッチング（§5）
│   ├── attach.py          リンク・ノードに属性を付ける（§4.3、§5）
│   ├── turns.py           右左折の規制・交差点名を抽出（§12.3、§12.4）
│   ├── dataset.py         データセットと manifest を書き出す（§12.2）
│   └── report.py          対応率・件数のレポート（§8-2）
├── api/
│   ├── graph.py           CSR グラフ・重み計算・Dijkstra（§7.1）
│   └── main.py            FastAPI（§7.2）
├── web/index.html         画面（§7.3）
└── data/                  生成物（git 管理外）
    └── dataset/{版}/      links / nodes / turns / manifest.json（§12.2）
```

---

## 12. ナビゲーションシステムの土台として

このリポジトリは、自転車ナビゲーションシステムをゼロから作るときの土台になる。そのため第1版から、次の方針で作る。

### 12.1 土台は「探索エンジン」ではなく「自転車向け道路データセット」

ナビで長く使われるのは、**KSJ のリンクごとに自転車向けの属性を付けたデータセット**のほうである。探索エンジンは、規模や速度の要件に応じて入れ替わる。そこで3つの層に分け、層の間はファイルの形式（§12.2）だけで結ぶ。

| 層 | 第1版 | ナビで想定される置き換え |
|---|---|---|
| データセット | `preprocess/` が作る parquet | 同じものを全国分作る |
| 探索エンジン | scipy の Dijkstra（ノードベース） | 辺ベースのグラフで右左折のコストを扱えるもの（§12.3）。自前実装（双方向 A*、Contraction Hierarchies など）か、既存エンジンへの書き出し |
| API・画面 | FastAPI ＋ 1枚の HTML | ナビアプリ・サーバー |

既存エンジンへの書き出しには前例がある。ksj-route-search-api には KSJ リンクを OSM PBF に書き出す `preprocess/export_osm_pbf.py` があり、同じ方法で GraphHopper・Valhalla などに渡せる。どちらにするかは、第1版で全国規模の速度を測ってから決める。

### 12.2 データセットの形

| ファイル | 1行 | 主な列 |
|---|---|---|
| `links.parquet` | 1リンク | `link_key`、始点・終点ノード、形状、長さ、KSJ 属性、`V_24`・`V_h`（24列）、`r` の元になる設置率、規制フラグ（通行禁止・一方通行の向き・時間帯）、各値の出典と推定かどうかのフラグ |
| `nodes.parquet` | 1ノード | `node_key`、座標、信号、一時停止、交差点名 |
| `turns.parquet` | 1つの右左折（進入リンク → ノード → 退出リンク） | 規制（禁止・指定方向）、自転車への適用、時間帯、出典 |
| `manifest.json` | 1版 | 入力データの版（KSJ・センサス・JARTIC の対象月）、処理のバージョン、件数、対応率 |

**安定 ID が要る。** KSJ の N13 にはリンクの ID が無く、ksj-route-search-api の `link_id` は行番号なので、作り直すたびに変わる。ナビでは、経路の保存、利用者からの報告、版ごとの差分の管理でリンクを指し示す必要がある。そこで次のように決める。

- `link_key`：端点座標（1e-7度）・長さ（0.1m）・2次メッシュ番号から作るハッシュ
- `node_key`：座標から作るハッシュ
- KSJ の版が変わって形状が変わったリンクは、新しい ID になる。旧版の ID との対応は §5.1 の線マッチングで作る

### 12.3 右左折（第2版で探索に使う）

ナビでは、右左折の禁止と右左折のコストを扱えないと、案内として成り立たない。

- **自転車は必ず二段階右折になる。** 道路交通法第34条第3項は、特定小型原動機付自転車等に、右折時に道路の左側端に寄って交差点の側端に沿って進むことを義務づけている。第18条は特定小型原動機付自転車等を「特定小型原動機付自転車及び軽車両」と定義しているので、自転車も含まれる（e-Gov 法令API、2026-07-21 施行版）。信号交差点で右折すると、信号待ちがおおむね2回になる
- **幹線道路を信号の無い交差点で横切る**のは危険。横切る道路の `f_t·(1 − r)` をコストにする案がある
- 抽出する規制（第1版から `turns.parquet` に入れる）：
  - `12` 指定方向外進行禁止：点規制。`進入方向(座標)` と `禁止する方向(座標)`・`指定する方向(座標)` から、進入リンクと退出リンクを決める
  - `83` 普通自転車の交差点進入禁止
  - `13` 車両進入禁止
  - いずれも §4.2 の判定で自転車への適用を決める

探索を辺ベースのグラフ（リンクを頂点、右左折を辺とする）に変えると、辺の数はおおむねリンク数 × 平均の退出方向数になる。第1版で規模を測って、エンジンを選ぶ材料にする。

### 12.4 案内に要る情報

| 情報 | 出どころ | 範囲 |
|---|---|---|
| 交差点名 | JARTIC の `交差点名称(踏切名含む)`（信号・規制の行にある） | 信号交差点の一部 |
| 道路名 | センサスの `路線名`（マッチしたリンク） | 国道・都道府県道 |
| 曲がる向きと角度 | KSJ の形状から計算 | 全リンク |

生活道路には名前が無い。案内は「〇m先を右」のように距離と向きで出すことになる。

### 12.5 全国への展開

- 対象都道府県は `config.toml` で指定する。コードに都県名を書かない
- **一方通行の向きのコードは都道府県ごとに違う**（§4.2。`1` が31都道府県、`2` が16都道府県で、静岡は混在）。都道府県ごとに OSM と突き合わせた検証結果を表で持つ。検証していない都道府県では、向きを決めつけず、一方通行のリンクを両方向とも大きく割り増しして避け、通るときは画面で「向き未検証」と示す
- 入力はどれも全国分ある（KSJ 160メッシュ、センサス全国、JARTIC 47都道府県）

### 12.6 更新の周期

| データ | 周期 | 対応 |
|---|---|---|
| JARTIC 交通規制情報 | 毎月（対象月の約2か月後に公開） | 規制だけ付け直す。道路網はそのまま |
| KSJ 道路 | 年1回程度 | 道路網から作り直す。`link_key` の対応表を作る |
| 道路交通センサス | 約5年ごと（次は R07） | 交通量・設置率を差し替える |

`manifest.json` に各データの版を記録し、どの版の組み合わせでデータセットを作ったかを追えるようにする。

### 12.7 利用条件

| データ | 条件 | 状態 |
|---|---|---|
| JARTIC 交通規制情報 | 商用利用可。出典の記載と、加工した旨の記載が必要。CC BY 4.0 と互換。利用者の行為について JARTIC は責任を負わない（[利用規約](https://www.jartic.or.jp/d/opendata/riyou_kiyaku.pdf) 第2条・第3条・第6条） | 確認済み |
| 道路交通センサス | 国交省サイトの利用ルール（PDL1.0）で、出典と加工の明示が必要。**形状は DRM 由来の可能性があり、国交省に照会する予定**（mlit-road-traffic-census-converter のローカルメモより）。本リポジトリは形状を配布せず、KSJ リンクに移した属性だけを使う | 形状の扱いは照会待ち |
| KSJ 道路（N13） | ksj-route-search-api は「測量法に基づく国土地理院長承認（使用）R 8JHs 85」を記載している。**ナビとして配布・提供する場合に承認が要るか、どの範囲かは未確認** | 要確認 |

### 12.8 安全に関わること

- JARTIC の規制は実際の標識と一致しない場合がある（仕様書の注意事項）。ナビの画面では、**実際の標識・標示に従う**ことを常に示す
- 推定値に頼っている区間（センサスの無い道路の交通量・歩道、推定した一方通行の向き）は、データセットにフラグで持ち、案内の画面で区別できるようにする

---

## 13. ナビ端末の構成

### 13.1 土台にする既存のシステム

利用者が2024年に試作した**オフライン地図表示システム**を土台にする（[MapLibre Meetup Japan #03 の発表資料](https://www.docswell.com/s/shi-works/KEX8L8-2024-11-10-174230)、配信データは [g-portable-upload-data](https://github.com/shiwaku/g-portable-upload-data)）。

| 項目 | 内容 |
|---|---|
| 本体 | Raspberry Pi 4 Model B（**メモリ 8GB**）、microSD 64GB（OS とデータで約32GB を使用）、モバイルバッテリー |
| 配信 | hostapd と dnsmasq で Wi-Fi アクセスポイント（SSID「UNVTPortable」）を立て、Apache か Nginx で `http://192.168.249.1/` を配信 |
| 地図データ | 国土地理院 最適化ベクトルタイル（PMTiles、15.8GB）、PLATEAU 建築物 LOD0（PMTiles、2.9GB）、国土地理院 標高タイル DEM10B（Terrain-RGB、8.7GB） |
| 表示 | 接続した Android スマホのブラウザ（MapLibre GL JS 4.5.0 ＋ PMTiles 2.11.0） |

**Pi 4 は地図を配るサーバーで、地図を描くのは接続したスマホ**、という分担になっている。

### 13.2 構成案

| | 案A：自己完結型 | **案B：今の構成を活かす型（先に作る）** |
|---|---|---|
| 画面 | Pi 4 に画面を付け、Chromium を全画面表示（キオスク） | ハンドルに付けたスマホ。今と同じく Pi 4 の Wi-Fi に接続する |
| Pi 4 の置き場 | ハンドル周り | バッグの中 |
| 測位 | GYSFDMAXB ＋ gpsd（§13.4） | 同じ |
| 経路探索 | Pi 4 の中 | Pi 4 の中 |
| 追加で要るもの | 直射日光の下で読める画面、防水、振動対策、電源の増強、MapLibre の描画性能の実測 | GPS と、経路探索・案内のソフトだけ |

案Bでは、画面の明るさ・タッチ操作・防水・バッテリーをスマホが担う。Pi 4 は「**通信なしで地図・経路探索・測位を提供する頭脳**」に徹する。案Aは、案Bで案内の中身が固まってから作る。表示は同じ HTML なので、ソフトはほぼそのまま移せる。

```
[GYSFDMAXB] → gpsd → 位置の中継（WebSocket） ─┐
                                               ├→ 画面：MapLibre ＋ 地理院PMTiles ＋ 経路・案内 → 音声・ブザー
[bicycle-route-search の API（Pi 4 内）]    ─┘
[Wi-Fi アクセスポイント（内蔵）]  ← スマホ（案B）
[USB Wi-Fi または LTE（任意）]    → JARTIC 月次更新の取り込み
```

### 13.3 Pi 4 に載せるもの

| 要素 | 見込み | 確かめること |
|---|---|---|
| 経路探索（§7） | 8GB は、ksj-route-search-api が全国の車の探索で使うメモリ（2.9GB）が載る大きさ。東京・埼玉の自転車用の道路網なら余裕がある見込み | 64bit 版 OS で scipy が動くこと。1回の探索にかかる時間（PC より遅くなる） |
| データセット（§12.2） | microSD の残り約32GB に収まる見込み | 実際のサイズ |
| 地図 | 今のタイル・スタイル・フォント・スプライトをそのまま使う | 案Aでは、Pi 4 の GPU で 2D 表示が滑らかに動くか。3D 建物・地形は切る前提 |

### 13.4 測位

- 受信機は raspi-gps-logger と同じ GYSFDMAXB（MTK3339）を使い、gpsd で読む
- **Pi 4 ではシリアルの設定が Pi Zero W と変わる可能性がある。** Bluetooth がシリアル端子を使う設定が既定なので、`/dev/serial0` の割り当てを確認する（未確認）
- **位置はブラウザの位置情報機能に頼らず、Pi 4 から WebSocket で渡す。** Chrome などのブラウザは、位置情報の機能を安全な接続（`https`、または `localhost`）でしか使わせないため、今の `http://192.168.249.1/` では `GeolocateControl` が動かない見込み。案Bのスマホの GPS を使いたい場合は、`https` での配信が別途要る
- GPS の位置は、ビルの谷間で道路からずれる。案内には、位置を道路網に当てはめる処理（マップマッチング）が要る。`link_key`（§12.2）で道路を指せるようにしておく

### 13.5 通信

- ナビ自体はオフラインで動く。通信を使うのは、JARTIC の月次更新（§12.6）を取り込むときと、探索をサーバーに任せたいとき
- **内蔵の Wi-Fi はアクセスポイントに使っている**ので、インターネットに同時につなぐには、USB の Wi-Fi アダプター（スマホのテザリングにつなぐ）か LTE ドングルを足す

### 13.6 案内

- 経路からの逸脱を検知し（経路から一定距離以上、一定時間以上離れたら）、Pi 4 の中で再探索する
- 曲がる地点の手前で、音声かブザー・振動で知らせる。**走行中に画面を注視することは自転車も禁止されている**（道路交通法第71条第5号の5。対象は「自動車、原動機付自転車又は自転車」。e-Gov 法令API、2026-07-21 施行版で確認）ため、画面は一瞬見て分かる表示（次の曲がる向き・残りの距離・交差点名）にする
- 曲がる向きと交差点名は §12.4 のデータを使う

### 13.7 進め方

| 段階 | 内容 | 分かること |
|---|---|---|
| 1 | Pi 4 に GYSFDMAXB をつなぎ、gpsd の位置を WebSocket で今の `index.html` に出す | Pi 4 のシリアル設定、測位の精度と更新間隔 |
| 2 | bicycle-route-search を Pi 4 で動かし、スマホの画面で経路を引く | 探索の速度とメモリ |
| 3 | 逸脱の検知・再探索・音での案内 | 案内として使えるか。実際に走って確かめる |
| 4 | 走行ログを記録し、GPS のずれと経路の良し悪しを検証する | マップマッチングの精度。おすすめ経路と、実際に選んで走った道の違い |
| 5 | 案A（画面付きの専用端末）を試す | 描画性能、電源、屋外での見やすさ |
